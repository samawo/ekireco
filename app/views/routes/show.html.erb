<% if @route.active %>
    <h1><%= @route.name %>(<%= @route.number %>駅)<% if user_signed_in? %>[<%= current_user.complete_routes.find_by(route_id: @route.id).number %>/<%= @route.number %>]<% end %></h1>
<% else %>
    <h1><%= @route.name %><廃路線>(<%= @route.number %>駅)<% if user_signed_in? %>[<%= current_user.complete_routes.find_by(route_id: @route.id).number %>/<%= @route.number %>]<% end %></h1>
<% end %>

<h4>所属駅一覧</h4>
<%= render 'layouts/station_index',stations: @stations  %>

    